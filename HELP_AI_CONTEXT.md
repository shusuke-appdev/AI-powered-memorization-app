# AI暗記カード 統合リファレンス (ヘルプAI用)

## 1. アプリ概要
AIを活用した穴埋め式フラッシュカードアプリ。Gemini APIによる問題生成とSM-2アルゴリズムによる復習間隔管理が特徴。
法律学習（司法試験、予備試験等）に最適化されている。

## 2. ユーザーガイド (操作手順)

### 2.1 開始設定
1. **アカウント作成**: ユーザー名(2文字+)、パスワード(4文字+)で登録。
2. **APIキー設定**: Google AI Studioでキーを取得し、サイドバー「APIキー設定」に入力。
   - *保存先*: `users` テーブル (暗号化されず保存されるため注意)
3. **ノルマ設定**: サイドバーで1日の上限枚数を設定 (デフォルト15枚)。

### 2.2 カード作成
1. **タブ**: 「📝 カードを追加」
2. **フロー**:
   - **カテゴリ選択**: 民法、刑法など8分類。
   - **テキスト解析**: 原文を入力し「解析」→ AIが文節分割。
   - **穴埋め作成**: 隠したい箇所をクリック (緑色反転)。1カード最大3箇所。
   - **保存**: 「デッキに保存」。原文も自動保存される。
   - **手動モード**: `【】`で囲んだ箇所を穴埋めにする機能あり。

### 2.3 復習 (本日のノルマ)
1. **タブ**: 「📚 本日のノルマ」
2. **学習プロセス**:
   - 穴埋め問題 (`[___]`) を見る。
   - 「答えを見る」をクリック。
   - **自己評価**: 4段階で評価し、次回スケジュールを決定。
     - `😰 忘れた` (0): 翌日 (カウントリセット)
     - `😓 難しい` (3): 間隔短縮
     - `🙂 普通` (4): 標準
     - `😊 簡単` (5): 間隔延長
3. **お気に入り**: 「☆」ボタンで原文単位で登録可能。

### 2.4 原文確認 & 聞き流し
- **原文確認**: ノルマ完了後、学習したカードの原文を一覧表示。文脈での再確認用。
- **聞き流しモード**: 「🎧 聞き流し」タブ。
   - カテゴリを選択して「再生リスト作成」。
   - **自動連続再生**: 原文カードをランダムに読み上げ (Web Speech API)。
   - 通勤・通学中の学習に最適。

### 2.5 データ管理
- **カード管理**: 「🗂️ カード管理」タブで検索・編集・削除。
- **統計**: 「📊 統計」タブで習得率、推移を確認。
- **バックアップ**: JSON形式でエクスポート可能。

## 3. 技術仕様詳細

### 3.1 AIロジック (Gemini Client)
- **モデル**: `gemini-2.5-flash`
- **プロンプトエンジニアリング**:
  - 文節分割 (`split_into_phrases`) に特化したfew-shotプロンプト。
  - **ルール**:
    - 助詞 (は, が, を...) は独立したブロックとして分割。
    - 句読点、括弧、丸数字も独立。
    - 複合語は意味のまとまり重視 (「AのB」は分割)。
  - **フォールバック**: API制限時 (`429`) やエラー時は、句読点ベースの簡易分割 (`simple_split`) に自動切り替え。

### 3.2 データベース構造 (Supabase: PostgreSQL)
#### `users`
- `id` (UUID): PK
- `password_hash`: bcryptハッシュ
- `api_key`: Gemini APIキー (Plain text)

> [!IMPORTANT]
> **セキュリティ設定 (Row Level Security)**
> 全テーブルでRLSが有効化されており、公開アクセス（anon key）は拒否されます。
> バックエンド（Streamlitアプリ）からのアクセスには、必ず **Service Role Key** を使用してください。

#### `cards`
- `id` (UUID): PK
- `source_id` (UUID): 原文カードへの外部キー
- `question`, `answer`: テキストデータ
- `ease_factor` (Float): SM-2難易度係数 (Initial: 2.5)
- `interval` (Int): 復習間隔日数
- `next_review` (Date): 次回予定日
- `is_favorite` (Bool): お気に入りフラグ

#### `source_cards`
- `id` (UUID): PK
- `source_text`: 原文全体

### 3.3 データ交換形式 (JSON Schema)
エクスポートデータ (`export_import.py`):
```json
{
  "version": "1.0",
  "exported_at": "YYYY-MM-DD",
  "cards": [
    {
      "question": "...", "answer": "...", "category": "...",
      "ease_factor": 2.5, "interval": 1, "repetitions": 0,
      "next_review": "YYYY-MM-DD"
    }
  ],
  "source_cards": [
    {
      "source_text": "...", "title": "...", "category": "..."
    }
  ]
}
```

### 3.4 アルゴリズム
- **SM-2**:
  - `Interval[n] = Interval[n-1] * EF`
  - `EF` は `1.3` を下限として、正解率により変動。
- **ハイブリッド出題選択**:
  - ノルマ数 (N) に対して、**苦手カード** (低EF) と **滞留カード** (古い日付) を約1:1で混合して出題リストを作成。
  - 同一 `source_id` のカードは1日1枚制限 (過学習防止)。

## 4. トラブルシューティング

### 4.1 よくあるエラー
- **"API_QUOTA_EXCEEDED"**: Gemini APIの無料枠制限。数分待機が必要。
- **"SpeechSynthesisError"** (音声): ブラウザが非対応、またはユーザー操作なしでの自動再生ブロック。再生ボタンを押して開始する。
- **インポートエラー**: JSON形式不正、または必須フィールド欠落。

### 4.2 仕様・制限
- **Web Speech API**: オフラインでも動作するが、ブラウザとOSの音声エンジンに依存。
- **大文字小文字**: ユーザー名ログイン時に区別されない (Case-insensitive)。
- **セッション**: 30日間有効 (Cookie)。
